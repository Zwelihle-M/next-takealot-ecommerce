import React from "react";

const HeroGradient = () => {
  return (
    <div
      style={{
        zIndex: -1,
      }}
    >
      <div className="hero-Gradient animate-spin rounded-full" />
    </div>
  );
};

export default HeroGradient;
